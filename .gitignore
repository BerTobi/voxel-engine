# Build artifacts (regenerable from source via the Makefile)
/build/
*.o
*.obj
*.exe.tmp

# Runtime save data (per-seed region files; not source)
/saves/

# Win32 diagnostic log (written next to the exe by plat_diag_init)
voxel_log.txt

# Editor / OS cruft
*~
*.swp
.DS_Store

# Intentionally TRACKED (do NOT ignore):
#   archive/      - preserved per-version executables (constant backups, by request)
#   screenshots/  - documentation captures
#   src/, *.md, Makefile, .gitignore
